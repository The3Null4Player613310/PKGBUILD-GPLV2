################################################################
#PKGBUILD-GPLV2: PKGBUILD
#Copyright © 2024 Allison Munn
#FULL COPYRIGHT NOTICE IS IN README
################################################################

# PACKAGE DETAILS
pkgname='';
pkgdesc='';
url='';
license='GPL-2.0-only';

# group='';
# conflicts=('foobar');
# provides=('foobar=0.0.0');
# replaces=('foobar');

# VERSION DETAILS
arch=('x86_64');
pkgver='0.0.0';
pkgrel=0;
# epoch=0;

# DEPENDENCIES
# depends=('dependency-a>=0.0.0' 'dependancy-b>=0.0.0');
# makedepends=('');
# checkdepends=('dependency-c>=0.0.0');
# optdepends=('dependency-d: feature support');

# protocol='https://'
# site='';
# source=("${pkgname}-${pkgver}.tar.gz::${protocol}${site}/${pkgname}-${pkgver}.tar.gz");
# sha512sums=(SKIP);

pre_install(packageVerNew)
{
}

post_install(packageVerNew)
{
}

pre_upgrade(packageVerNew, packageVerOld)
{
}

post_upgrade(packageVerNew, packageVerOld)
{
}

pre_remove(packageVerOld)
{
}

post_remove(packageVerOld)
{
}
